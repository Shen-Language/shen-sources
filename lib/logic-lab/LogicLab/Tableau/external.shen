(define fol-external-symbols
  {--> (list symbol)}
  -> (append (external stlib) [~ v & => <=> all exists prop d-rule hyp vl &l =>r =>l  
                               <=>l ~l alll existsl valid dn contradiction demorgan1 
                               demorgan2 ~=> ~<=> indirect-proof sequent]))